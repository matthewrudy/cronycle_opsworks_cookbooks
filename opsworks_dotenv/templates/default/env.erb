# This file is generated by the opsworks_dotenv cookbook. Update it by setting the
# stack's custom JSON and executing the opsworks_dotenv::configure recipe.

<% @env.sort.each do |key, val| %>
<%= key.to_s %>: <%= (val && val.to_s.inspect) %>
<% end %>
